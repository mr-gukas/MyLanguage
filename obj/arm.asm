CALL :main
HLT

main:
PUSH 0
//ded
POP [0+RAX]

INP
POP [0+RAX]
PUSH 0
//otec
POP [1+RAX]

INP
POP [1+RAX]
PUSH 0
//brat
POP [2+RAX]

INP
POP [2+RAX]
PUSH 0
//greatded
POP [3+RAX]

PUSH 0
//greatbab
POP [4+RAX]

PUSH [0+RAX]
PUSH 0

JE :else_0
PUSH RAX
PUSH 5
ADD
POP RAX

PUSH [-4+RAX]
PUSH 2
POW
PUSH 4
PUSH [-5+RAX]
MUL
PUSH [-3+RAX]
MUL
SUB
//godfather
POP [0+RAX]

PUSH [0+RAX]
PUSH 0
ISBT
PUSH 0

JE :else_0
PUSH RAX
PUSH 1
ADD
POP RAX

PUSH -1
PUSH [-5+RAX]
MUL
PUSH [-1+RAX]
PUSH 0.5
POW
ADD
PUSH 2
PUSH [-6+RAX]
MUL
DIV
//greatded
POP [-3+RAX]
PUSH [-3+RAX]
OUT
PUSH -1
PUSH [-5+RAX]
MUL
PUSH [-1+RAX]
PUSH 0.5
POW
SUB
PUSH 2
PUSH [-6+RAX]
MUL
DIV
//greatbab
POP [-2+RAX]
PUSH [-2+RAX]
OUT
PUSH 1
POP RCX
PUSH RAX
PUSH 1
SUB
POP RAX

RET

PUSH 5
POP RAX

JMP :if_0

else_0:
PUSH RAX
PUSH 1
ADD
POP RAX

PUSH 1
POP RCX
PUSH RAX
PUSH 1
SUB
POP RAX

RET

PUSH 5
POP RAX

if_0:
PUSH 0
POP RAX

JMP :if_1

else_1:
PUSH RAX
PUSH 5
ADD
POP RAX

PUSH [-4+RAX]
PUSH 0

JE :else_1
PUSH RAX
PUSH 0
ADD
POP RAX

PUSH -1
PUSH [-3+RAX]
MUL
PUSH [-4+RAX]
DIV
//greatded
POP [-2+RAX]
PUSH [-2+RAX]
OUT
PUSH 1
POP RCX
PUSH RAX
PUSH 0
SUB
POP RAX

RET

PUSH 5
POP RAX

JMP :if_1

else_1:
PUSH RAX
PUSH 0
ADD
POP RAX

PUSH 1
POP RCX
PUSH RAX
PUSH 0
SUB
POP RAX

RET

PUSH 5
POP RAX

if_1:
PUSH 0
POP RAX

if_2:
PUSH 0

RET

